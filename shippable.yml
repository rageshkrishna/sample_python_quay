language: python

python:
  - 3.4
  - 2.7

services:
 - redis
 
env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE

build:
  #pre_ci:
   # - docker build -t=quay.io/shippablequay/sample_py_quay .

  

  ci:
    - ps -eax
    - ls -al

  post_ci:
    - docker build -t=quay.io/shippablequay/pyt-public .
    - docker push quay.io/shippablequay/pyt-public
    - docker build --rm -t=quay.io/sshippablequay/pyt-public:test .
    - docker build --rm -t=shippablequay/pyt-public:qa .
    - docker push quay.io/shippablequay/pyt-public:test
    - docker push quay.io/shippablequay/pyt-public:qa



integrations:
  hub:
    - integrationName: "ship-quay"
      type: "quay.io"
      
